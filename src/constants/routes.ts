import {APP_ROUTES_TYPE} from "@/types";

// Here we can write global constants that can be used in the whole project

// FIXME: Define the app routes, footer links, and other constants here
export const APP_ROUTES: APP_ROUTES_TYPE[] = [];
